local _modules = {
    {
        name = "ludo",
        version = "0.0.1-dev",
        dependencies = {
            "licht.core",
            "licht.rhi",
        }
    }
}

return {
    modules = _modules
}
